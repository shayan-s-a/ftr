<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>CanUserSeeThisRecord__c</fullName>
    <externalId>false</externalId>
    <formula>IF(
OR(
$User.View_All_Contacts__c = TRUE,
IF(
CONTAINS(CreatedBy.UserRole.Name, &apos;B2B&apos;),
IF(
CONTAINS($UserRole.Name, &apos;B2B&apos;),
TRUE,
FALSE
),
IF(
NOT(CONTAINS($UserRole.Name, &apos;B2B&apos;)),
TRUE,
FALSE
)
)
),
TRUE,
FALSE
)</formula>
    <label>Can logged in user see this record?</label>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>
