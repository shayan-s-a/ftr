vlocity.cardframework.registerModule.controller('vlcShowUniController', [
    '$scope', '$http',
    function($scope, $http) {
        // console.log($scope.control.response.ServiceAccountId);
        // $scope.$watch('bpTree.response.ShowUNI', (newVal) => {
        //     if ($scope.loaded || !newVal) return;
            
        //     //console.warn($scope.bpTree.response.ShowUNIs)
        //     $scope.unis = $scope.bpTree.response.ShowUNIs;
        //     console.log($scope.control);
            
        //     // TODO: Remove once a better solution is found;
        //     //$scope.locationId = $scope.$parent.$parent.control.response.Access_ServiceAccountId;
        //     $scope.loaded = true;
        // })
}]);